The URL for the request.

The scheme defaults to 'http://' if the url does not include one.

You can also use a shorthand for localhost

  $ chrome :3000                # => http://localhost:3000
  $ chrome :/foo                # => http://localhost/foo
